# デバッグ用の LINE ログイン情報（debug 用途のみ）。
# `cp .env.example .env` でコピーして値を設定してください。
# .env は .gitignore 済みなのでコミットされません。

user=your-line-email@example.com
password=your-line-password

# サーバーの待受ポート（任意、未指定時は 3000）
# PORT=3000
